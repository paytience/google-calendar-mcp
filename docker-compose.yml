services:
  mcp:
    build: .
    command: ["node", "dist/index.js"]
    container_name: google-calendar-mcp
    environment:
      - GOOGLE_CALENDAR_MCP_API_KEY=${GOOGLE_CALENDAR_MCP_API_KEY}
    stdin_open: true
    restart: unless-stopped

